import React from 'react';
import Body from '../components/Body';



const Home = ({tick}) => {
    

    return (
        <section>
            <Body tick={tick}/>
        </section>
    );
};

export default Home;